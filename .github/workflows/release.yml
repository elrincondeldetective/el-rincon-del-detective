name: Publicar en GitHub por etiquetas

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

concurrency:
  group: release-on-tag-${{ github.ref }}
  cancel-in-progress: false

jobs:
  publish:
    name: Publicar en GitHub por etiqueta
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout (historial completo para anotaciones de etiquetas)
        uses: actions/checkout@v4
<<<<<<< HEAD
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Actualizar etiquetas (force)
        run: |
          set -euo pipefail
          git fetch --tags --force

=======
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Actualizar etiquetas (force)
        run: |
          set -euo pipefail
          git fetch --tags --force

      - name: Preparar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: apps/el-rincon-del-detective/frontend/package-lock.json

      - name: Instalar dependencias
        working-directory: ./apps/el-rincon-del-detective/frontend
        run: npm ci

      - name: Compilar aplicacion
        working-directory: ./apps/el-rincon-del-detective/frontend
        run: npm run build

      - name: Empaquetar artefacto
        id: artifact
        shell: bash
        run: |
          set -euo pipefail
          tag="${GITHUB_REF_NAME}"
          name="el-rincon-del-detective-${tag}.zip"
          cd apps/el-rincon-del-detective/frontend

          if [ ! -d "out" ]; then
            echo "No se encontro la carpeta out. Revisa next.config.ts" >&2
            exit 1
          fi

          zip -r "../${name}" out
          echo "artifact_path=apps/el-rincon-del-detective/${name}" >> "$GITHUB_OUTPUT"
          echo "artifact_name=${name}" >> "$GITHUB_OUTPUT"

>>>>>>> 899527d (ci(release): publicar y desplegar por etiquetas `v*`)
      - name: Leer mensaje de etiqueta (notas de publicacion)
        id: notes
        shell: bash
        run: |
          set -euo pipefail
          tag="${GITHUB_REF_NAME}"

          # Leer el contenido del tag anotado (notas guardadas con -m/-F)
          body="$(git for-each-ref "refs/tags/${tag}" --format='%(contents)')"

          # Fallback por si el tag fuera lightweight (sin notas)
          if [ -z "$body" ]; then
            body="Publicacion ${tag}"
          fi

          {
            echo "body<<'EOF'"
            echo "$body"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

          if [[ "$tag" == *"-rc"* || "$tag" == *"-beta"* || "$tag" == *"-alpha"* ]]; then
            echo "prerelease=true" >> "$GITHUB_OUTPUT"
          else
            echo "prerelease=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Crear publicacion en GitHub
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          body: ${{ steps.notes.outputs.body }}
          prerelease: ${{ steps.notes.outputs.prerelease }}
          draft: false
          generate_release_notes: false
<<<<<<< HEAD
=======
          files: ${{ steps.artifact.outputs.artifact_path }}
>>>>>>> 899527d (ci(release): publicar y desplegar por etiquetas `v*`)
